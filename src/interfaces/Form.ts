export interface IForm {
    zipCode: string | null,
    address : string | null,
    number : number | null,
    city: string | null,
    neighborhood: string | null,
    name: string | null,
    lastName: string | null,
    email: string | null,
    phone: string | null
}
